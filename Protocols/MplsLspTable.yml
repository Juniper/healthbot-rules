---
MplsLspTable:
  rpc: get-mpls-lsp-information
  args:
    detail: True
  item: rsvp-session-data/rsvp-session/mpls-lsp | rsvp-session-data/rsvp-session
  key: 
    - name
  view: MplsLspView

MplsLspView:
  fields:
    destination-address: destination-address
    source-address: source-address
    lsp-state: lsp-state
    session-type: ../../session-type | ../session-type
