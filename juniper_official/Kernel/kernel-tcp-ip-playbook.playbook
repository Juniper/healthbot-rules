healthbot {
    playbook kernel-tcp-ip-playbook {
        rules kernel.tcpip/check-iri-conn-keepalive-dropped;
        description "This playbook encapsulates rules related to kernel tcp ip parameters";
        synopsis "kernel tcp ip parameters";
    }
}
