healthbot {
    playbook security-kpis-playbook {
        rules [ security/check-veriexec-status security/check-veriexec-status-iagent ];
        description "This playbook all the rules keeping a check on security parameters from the topic security" ;
        synopsis "security parameters of device";
    }
}
