---
ProcessMemoryTable:
    command: show system processes extensive
    key: cmd
    key_items:
        - chassisd
        - pfed
        - dcd
        - dfwd
        - snmpd
        - mib2d
        - cosd
        - rpd
        - mgd
        - eventd
    view: ProcessMemoryView
ProcessMemoryView:
    regex:
        size: '.* \S +(\d+(K|M))\s+'
        res: '(\d+(K|M))'
        wcpu: '.* (\d+\.\d+)%'
        cmd: '\w+'
