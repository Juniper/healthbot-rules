---
DBSizeTable:
  rpc: get-database-usage
  item: .
  key:
  view: DBSizeView
 
DBSizeView:
  fields:
    maxsize: maximum-size
    cursize: current-size
    actsize: actual-usage
    avaisize: available-space
