---
DBSizeTable:
  rpc: get-database-usage
  item: .
  key:
  view: DBSizeView
 
DBSizeView:
  fields:
    maxsize: {maximum-size: Search=(\d+\.\d+)}
    cursize: {current-size: Search=(\d+\.\d+)}
    actsize: {actual-usage: Search=(\d+\.\d+)}
    avaisize: {available-space: Search=(\d+\.\d+)}
