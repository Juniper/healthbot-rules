---
ProcessMemoryTable:
    command: show system processes extensive
    key: cmd
    view: ProcessMemoryView
ProcessMemoryView:
    regex:
        size: '.* \S +(\d+(K|M))\s+'
        res: '(\d+(K|M))'
        wcpu: '.* (\d+\.\d+)%'
        cmd: '\w+'
