---
VMhostMemoryTable:
  command: show vmhost resource-usage
  key: memory
  title: 'Memory Usage'
  view: VMhostMemoryView

VMhostMemoryView:
  regex:
    memory: 'Mem:'
    total: (\d+)
    used: (\d+)
    free: (\d+)
    shared: (\d+)
    buffers: (\d+)
    cached: (\d+)

