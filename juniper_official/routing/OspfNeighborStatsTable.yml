---
OspfNeighborStatsTable:
  rpc: get-ospf-neighbor-information
  args:
    extensive: !!bool True
    instance: all
  command: /usr/sbin/cli -c 'show ospf neighbor instance all extensive | display xml | save /tmp/insights-ospf-neighbor.xml'
  item: ospf-instance-neighbor/ospf-neighbor
  key:
    - interface-name
    - neighbor-id
    - neighbor-address
    - ../ospf-instance-name
  view: OspfNeighborStatsView
  compress: True
  responseFilePath: /tmp/insights-ospf-neighbor.xml

OspfNeighborStatsView:
  fields:
    ospf-neighbor-state: ospf-neighbor-state
    neighbor-address: neighbor-address
    activity-timer: activity-timer
    dr-address: dr-address
    bdr-address: bdr-address
    neighbor-up-time: neighbor-up-time
    neighbor-adjacency-time: neighbor-adjacency-time
    instance-name: ../ospf-instance-name
    neighbor-id: neighbor-id
    interface-name: interface-name

FilterQuery:
  Fields:
    - XPath: "rpc-reply/ospf-neighbor-information-all/ospf-instance-neighbor/ospf-neighbor/ospf-neighbor-state"
    

xslt: "<xsl:stylesheet version=\"1.0\"
 xmlns:xsl=\"http://www.w3.org/1999/XSL/Transform\"
 xmlns:ns=\"some:ns\">
 <xsl:output omit-xml-declaration=\"yes\" indent=\"yes\"/>
 <xsl:strip-space elements=\"*\"/>
 <ns:WhiteList>
      <name>interface-name</name>
      <name>neighbor-id</name>
      <name>ospf-neighbor-state</name>
      <name>neighbor-address</name>
      <name>dr-address</name>
      <name>bdr-address</name>
      <name>ospf-instance-name</name>
 </ns:WhiteList>
 <ns:blacklist>
      <name>neighbor-up-time</name>
      <name>neighbor-adjacency-time</name>
 </ns:blacklist>
 <xsl:template match=\"node()|@*\">
      <xsl:copy>
             <xsl:apply-templates select=\"node()|@*\"/>
      </xsl:copy>
 </xsl:template>
 <xsl:template match=
  \"*[not(descendant-or-self::*[name()=document('')/*/ns:WhiteList/*]) or (self::*[name()=document('')/*/ns:blacklist/*])]\"/>
 </xsl:stylesheet>"
